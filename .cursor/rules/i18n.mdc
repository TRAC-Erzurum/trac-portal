---
alwaysApply: false
globs: trac-portal-ui/**/*.ts, trac-portal-ui/**/*.vue, trac-portal-api/src/**/*.ts
---
# i18n

- No hardcoded text: `{{ t('key') }}` with `const { t } = useI18n()`
- TR/EN must stay in sync
- Errors: `toast.error(translateError(error.message))`
