---
alwaysApply: false
globs: trac-portal-ui/**/*.ts, trac-portal-ui/**/*.vue, trac-portal-api/src/**/*.ts
---
# API Patterns

- Use `@/lib/api.ts` typed fetch wrapper
- JWT in httpOnly cookie, auto-logout on 401
- Backend returns i18n keys → frontend uses `translateError()`
